name = "voip"
main = "worker.js"
compatibility_date = "2024-09-23"

# Durable Objects binding — the signaling room
[[durable_objects.bindings]]
name = "SIGNALING_ROOM"
class_name = "SignalingRoom"

[[migrations]]
tag = "v1"
new_classes = ["SignalingRoom"]
